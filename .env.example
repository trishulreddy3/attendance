# Copy this file to .env and fill in your values.
# NEVER commit .env to Git — set these in Vercel dashboard instead.

# Live backend (Render) — use this for production
VITE_API_BASE_URL=https://attendance-dhvi.onrender.com/api
VITE_WS_URL=wss://attendance-dhvi.onrender.com/ws

# Local backend — uncomment for local development
# VITE_API_BASE_URL=http://localhost:8080/api
# VITE_WS_URL=ws://localhost:8080/ws
